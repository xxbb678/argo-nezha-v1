services:
  argo-nezha-v1:
    image: ghcr.io/xxbb678/argo-nezha-v1:latest
    container_name: argo-nezha-v1
    restart: unless-stopped
    ports:
      - "443:443"
    env_file:
      - .env
    volumes:
      - ./dashboard:/dashboard/data
